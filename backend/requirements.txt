# ICB Platform - backend (FastAPI). PostgreSQL-only. Python 3.12+ (3.14 supported).
# Pins reflect the proven-working Cost Calculator venv; psycopg / alembic /
# pydantic-settings added for Phase 1 (WO v4.12).
# Intentionally excluded vs. the legacy app:
#   - PyMySQL   -> PostgreSQL-only in the monorepo
#   - gunicorn  -> uvicorn locally; Passenger runs the WSGI app directly on cPanel
#   - weasyprint-> not present in the working local venv; reportlab/pypdf is the
#                  active PDF path. (Re-add with GTK libs if HTML->PDF is needed.)
# (a2wsgi was excluded at v4.12; RE-ADDED below for the faje.co.za cPanel/Passenger deploy — WO v4.30 §3.2.)

# --- Web framework ---
fastapi==0.115.12
starlette==0.46.2
uvicorn==0.42.0
python-multipart==0.0.22
Jinja2==3.1.6
sse-starlette==1.8.2

# --- Config / validation ---
pydantic==2.12.5
pydantic-settings>=2.5,<3

# --- Database (PostgreSQL only) ---
SQLAlchemy==2.0.48
psycopg[binary]>=3.2,<4
alembic>=1.13,<2

# --- Auth / crypto ---
passlib==1.7.4
bcrypt==4.0.1
cryptography==46.0.6

# --- Excel import ---
openpyxl==3.1.5
xlrd==2.0.2

# --- PDF generation (reportlab/pypdf path) ---
reportlab==4.4.10
pypdf==4.0.1
pypdfium2==5.8.0
pillow==12.2.0
svglib==1.6.0

# --- AI Help assistant ---
anthropic==0.104.1

# --- Utilities ---
python-dotenv==1.2.2

# --- Deploy: cPanel/Passenger WSGI bridge (faje.co.za only; WO v4.30 §3.2) ---
# Passenger speaks WSGI; backend/passenger_wsgi.py wraps the ASGI app via a2wsgi.ASGIMiddleware.
# Pinned to the version the legacy GRP-Costing-System ran (its app/requirements.txt). Not used by the
# local uvicorn dev server or CI's test run — only the cPanel deploy imports passenger_wsgi.
a2wsgi==1.10.7
