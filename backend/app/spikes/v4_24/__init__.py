"""WO v4.24 — BOM rule-engine spike (Vacuum Materials × Freezer × job 32735).

EXPLORATORY SPIKE CODE. Not integrated into MES read paths; reads icb_sap.OITM
read-only (ADR 0013); no persistence. May be deleted by a future cleanup WO if not
promoted. See docs/spikes/v4.24-bom-rule-engine-spike.md.
"""
